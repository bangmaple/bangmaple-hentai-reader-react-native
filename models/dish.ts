export default interface Dish {
    id?: number,
    name?: string,
    category?: string,
    label?: string,
    image?: string,
    price?: string,
    featured?: boolean,
    description?: string
}
